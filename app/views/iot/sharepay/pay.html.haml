.iot.container.pay
  %h1.title.text-center 線上繳款
  .row-header
    .title.black-color
      付款資訊
      %span.helper.danger-color 請先確認以下資訊再進行付款
  .row-body
    .payment-detail.bg.light-blue
      %table
        %tbody
          %tr
            %th 訂單編號：
            %td= @payment.resource_app_order_no
          %tr
            %th 商品名稱：
            %td= @payment.product_name
          %tr
            %th 商品介紹：
            %td= @payment.product_desc
          %tr
            %th 商店名稱：
            %td 收多易
          %tr
            %th 應付金額：
            %td= @payment.formatted_price
  .row-header
    .title.black-color
      聯絡資訊
      %span.helper.danger-color 為了避免日後交易問題，我們需要請您註冊帳號，留下聯絡方式
  .row-body
    .contact-detail
      %table
        %tbody
          %tr
            %th 聯絡帳號：
            %td= @payment.user_login_as
          %tr
            %th 聯絡人：
            %td= @payment.current_user['name']
          %tr
            %th 聯絡信箱：
            %td= @payment.email
          %tr
            %th 聯絡手機：
            %td= @payment.phone

    = form_tag @form.redirect, method: :post do |f|
      = hidden_field_tag :data, @form.data
      = hidden_field_tag :mac, @form.mac
      = hidden_field_tag :ksn, @form.ksn
      = button_tag '確認付款', type: 'submit', class: 'button primary-button'
