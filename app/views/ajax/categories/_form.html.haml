= form_for @ajax_category do |f|
  - if @ajax_category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@ajax_category.errors.count, "error")} prohibited this ajax_category from being saved:"
      %ul
        - @ajax_category.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
