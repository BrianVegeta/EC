import React, { PropTypes } from 'react';

const ContactRequester = () => (
  <div styleName="container">
    <div styleName="user">-</div>
    <button className="default-button" styleName="contact">
      <span styleName="icon" />
    </button>
  </div>
);

export default ContactRequester;
